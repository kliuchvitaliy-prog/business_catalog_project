import React from 'react'
import ReactDOM from 'react-dom/client'
import CapitalCatalog from './CapitalCatalog.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CapitalCatalog />
  </React.StrictMode>,
)
